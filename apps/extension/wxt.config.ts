import { defineConfig } from "wxt";

export default defineConfig({
  modules: ["@wxt-dev/module-react"],
  manifest: {
    name: "Retrace",
    description: "Capture and organize research context across the web."
  }
});
